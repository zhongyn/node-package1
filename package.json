{
  "name": "test-lib",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/index.js",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "babel src --out-dir lib --copy-files",
    "prepare": "echo 'prepare test-lib' && npm run build",
    "clean": "rimraf lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.7.7",
    "rimraf": "^2.5.4"
  },
  "author": "",
  "license": "ISC"
}
